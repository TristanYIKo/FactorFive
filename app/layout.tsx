import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FactorFive - Intelligent Stock Analysis",
  description: "The intelligent 5-factor stock analysis engine. Analyze stocks with Growth, Profitability, Valuation, Quality, and Analyst Predictions.",
  keywords: ["stock analysis", "factor investing", "stock screener", "fundamental analysis", "stock research"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
